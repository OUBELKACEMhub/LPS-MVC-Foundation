{# App/templates/layouts/base.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShopEasy - {% block title %}Fidélité{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 flex flex-col min-h-screen">
    
    <header class="bg-blue-600 text-white shadow-lg">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            
            <a href="index.php?page=home" class="text-2xl font-bold flex items-center gap-2">
                🛍️ ShopEasy <span class="text-sm bg-blue-800 px-2 py-1 rounded">Loyalty</span>
            </a>

            <nav>
                <ul class="flex space-x-6">
                    <li><a href="index.php?page=dashboard" class="hover:text-blue-200 transition">Mon Solde</a></li>
                    <li><a href="index.php?page=rewards" class="hover:text-blue-200 transition">Récompenses</a></li>
                    <li><a href="index.php?page=history" class="hover:text-blue-200 transition">Historique</a></li>
                </ul>
            </nav>

            <div class="flex items-center gap-4">
                <span class="text-sm">
                    Bonjour, <strong>{{ user.name|default('Invité') }}</strong>
                </span>
                
                {% if user.is_logged is defined and user.is_logged %}
                    <a href="/logout" class="bg-red-500 hover:bg-red-600 px-4 py-2 rounded text-sm transition">
                        Déconnexion
                    </a>
                {% else %}
                    <a href="/login" class="bg-green-500 hover:bg-green-600 px-4 py-2 rounded text-sm transition">
                        Connexion
                    </a>
                {% endif %}
            </div>
        </div>
    </header>

    <main class="flex-grow container mx-auto px-4 py-8">
        {% block body %}
        {% endblock %}
    </main>

    <footer class="bg-gray-800 text-gray-300 py-6 mt-auto">
        <div class="container mx-auto px-4 text-center">
            <p class="mb-2">&copy; {{ "now"|date("Y") }} ShopEasy. Tous droits réservés.</p>
        </div>
    </footer>

</body>
</html>